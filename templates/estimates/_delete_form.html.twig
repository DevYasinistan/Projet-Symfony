<form method="post" action="{{ path('estimates_delete', {'id': estimate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estimate.id) }}">
    <button class="btn btn-lg btn-primary">Supprimer</button>
</form>
