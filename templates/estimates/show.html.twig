{% extends 'base.html.twig' %}

{% block title %}Estimates{% endblock %}

{% block body %}
    <h1>Estimates</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estimate.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ estimate.email }}</td>
            </tr>
            <tr>
                <th>Phonenumber</th>
                <td>{{ estimate.phonenumber }}</td>
            </tr>
            <tr>
                <th>Travaux</th>
                <td>{{ estimate.travaux }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ estimate.pays }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ estimate.adresse }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ estimate.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estimates_index') }}">back to list</a>

    <a href="{{ path('estimates_edit', {'id': estimate.id}) }}">edit</a>

    {{ include('estimates/_delete_form.html.twig') }}
{% endblock %}
